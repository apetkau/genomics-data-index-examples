name: gdi
channels:
  - conda-forge
  - bioconda
  - defaults
dependencies:
  - bedtools
  - pyqt
  - iqtree
  - python=3.8
  - bcftools
  - htslib
  - ete3
  - snpeff
  - pip
  - pip:
    - genomics-data-index==0.2.0
prefix: /home/apetkau/miniconda3/envs/gdi
